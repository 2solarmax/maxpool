# Maxpool — open issues & roadmap

Living tracker for in-flight work. One core issue at a time. Newest status on top of each item.

## Core philosophy (the goal everything serves)

**Avoid being rate-limited.** The way to do that: keep as many accounts *healthy* as possible and spread load so requests parallelize across them. Concentrating load on one account is what triggers short-term (per-session/per-minute) rate limits.

- **Default mode = healthy spread.** Distribute across all non-exhausted accounts to keep everyone healthy and maximize parallelism. Don't pick "which accounts to starve."
- **Use-it-or-lose-it = a LATE, secondary signal.** Only lean hard into an account when its *binding* window (session 5h or weekly 7d — whichever is most constraining) is genuinely about to reset soon; then drain that soon-to-reset quota before it's wasted.
- Never let a *raw-healthy* account (lots of real quota left, reset far off) get benched to last-resort purely because of pace.

---

## OPEN

### 1. Routing: spread-to-stay-healthy; use-it-or-lose-it only near reset  ⟵ CORE
**Status:** Phase 1 SHIPPED (v1.2.0), Phase 2 SHIPPED (v1.4.0). Phase 3 CLOSED 2026-06-27 (not worth building — see below).
**Problem:** The scheduler over-weighted pace + near-reset (`scarcity×6` was the dominant term), so it *concentrated* load on the single soonest-to-reset account and benched raw-healthy accounts (mk@gomokka at 69% raw) to last-resort "critical" via pace. Concentration is exactly what triggers the short-term throttle — the opposite of the goal.
**Design:** 3-advocate council + lead-architect synthesis (2026-06-26). Continuous, load-gated additive scoring — no mode switches. Phased "prove-it" rollout.
**Phase 1 (DONE, v1.2.0):**
- In-flight concurrency is now the DOMINANT score term (`concurrencyWeight=2`) + a steep per-account soft cap (`capPenaltyWeight=10` past `perAccountConcurrencyTarget D=3`) so no account absorbs a deep burst.
- Burn-pace demoted from `×6` dominant to a soft `paceCostWeight=1.5` de-preference (never a bench).
- Eligibility gates on RAW weekly state (`_weeklyRawState`), so a raw-healthy fast-burner stays in the spread pool.
- Screenshot-replay regression test: 6 concurrent over the 4-account state fans out across max@dubner.io + mk@gomokka, no account absorbs the burst.
- `D=3` chosen from observed evidence (max@dubner.io throttled ~6 concurrent); tunable.
**Phase 2 — safe-point session rebalancing (DONE, v1.4.0):**
- A bound session no longer sticks to a HOT account forever. `_selectNext` migrates it to a much-healthier account, but ONLY on a **thinking-safe** request (per-request body carries no signed `thinking` block; fail-closed on unparsed bodies) so Anthropic's non-retryable "invalid signature" can never fire.
- Flap-stable: HOT = `_isNearQuota` (weekly reserve/critical or 5h-cap, NOT live in-flight); migrate only to a `normal/soft/unknown`-weekly alt that is ≥2× cheaper AND a strictly healthier tier; suppressed during queue admission; `_bindSession` re-homes on a same-priority *choice* migration but not on failover (snap-back-on-recovery preserved).
- This is the fix for "I added an account mid-session but it didn't get picked up": added/cooled capacity now drains in-progress hot sessions at their next safe turn.
- Pre-mortem (9 failure modes) + focused adversarial review (SHIP); red→green tests for migrate-when-safe-and-hot, never-on-signed-thinking, fail-closed-unparsed, never-when-healthy, queue-suppressed, no-flap, describeRequest fail-closed.
**Phase 3 — CLOSED 2026-06-27 (not worth building; the premise didn't survive a code read):**
- ~~Per-account `throttlePressure` so the fleet steers around a freshly-429'd account instead of a "single global pause".~~ FALSE PREMISE: a single 429 already benches only that account (`markRateLimited`, per-account). The global pause (`shouldPromoteUpstreamFailure`) opens ONLY when every eligible account fails the same request AND no healthy account is untried/recently-succeeded — those veto it. So healthy accounts are not swept into it. The only marginal add (de-prefer an account that JUST recovered) is minor tuning, not a correctness gap.
- ~~Herd-jitter on the rebalance burst-drain.~~ Only bites with a much larger pool; already damped by the score-margin backpressure. Not worth it at ~22 accounts.
- The "defer pending live observation" framing was hollow — nobody watches a passive metric. The TUI's `Anthropic upstream throttled` line + throttle count IS the live signal; if it recurs WITH healthy accounts idle, the fix would be to the global-pause/probe-recovery window, not these scoring tweaks. Re-open only against a concrete observed regression.
**Open knob (kept):** `D` (per-account concurrency where Anthropic 429s). Default 3; tune from the TUI throttle signal if 429s cluster at a given depth.

### 2. Hold the session on rate-limit instead of killing it (+ routing-oracle correctness) — DONE
**Status:** SHIPPED (v1.3.0, branch feat/hold-session-and-routing merged to main). 165 tests.
**What shipped:** A rate-limited STREAMING session is now HELD ALIVE on an SSE heartbeat (up to `streamHoldMaxMs`, 7d) and resumed the instant any account frees — never killed. The hold-vs-error oracle (`nextRetryForRequest`/`_retryInfo`) error-fasts ONLY on genuinely-unrecoverable states (all accounts logged out / weekly-exhausted with unknown reset / no eligible route), and HOLDS finite for everything recoverable (weekly-critical last-resort, 5h cap, rate-limit, cooldown, concurrency cap). Retry-after now reflects the SOONEST real recovery (3-bucket min-merge), not a far weekly reset.
**Rigor:** 5 full code-review council rounds + focused adversarial passes; every confirmed finding fixed with a red-before/green-after test. Fixes included: session-kill on weekly-critical+unknown-reset, fairness-gate starvation, misleading multi-day retry-after, auth-dead spin, Bug-A heartbeat SSE interleave, post-resume-failover drop, lease-leak on client disconnect, healthy-concurrency-cap symmetry + 15-min bound, non-SSE-resume framing, EPIPE ghost eviction.

### 3. Remove the import mechanic (browser login only) — DONE
**Status:** SHIPPED (commit b6353fa on main). CLI `import` + TUI `i` + `importCredentials`/Keychain read + the upsert path + tests removed; no import-mechanic refs remain in src. Browser `maxpool login` (+ `login --api`) is the only add-account path.
**Problem:** The Keychain `import` path (a fallback I added) snapshots the CLI's own OAuth token, creating a shared-credential account that breaks when the CLI rotates the token (this is what kept erroring max@dubner.io). The original fork's browser login worked for all accounts.
**Plan:** remove CLI `import` + TUI `i` "Import Claude login" + `importCredentials`/Keychain read + the upsert-via-import path + tests + docs. Keep `maxpool login` (browser) and `login --api`.

### 4. max@dubner.io stability
**Status:** solution known; blocked on a one-time user action.
**Fix:** re-add max@dubner.io via **browser login** (TUI `a`→`l` on ≥1.0.5, or `maxpool login`) so maxpool holds its *own* independent grant, decoupled from the CLI's Keychain token. Resolves once done. Tied to #3.

---

### 5. TUI clarity: "throttled" and "Load X/Y" under-communicate — DONE
**Status:** SHIPPED (v1.3.1).
- **"throttled"** now renders `throttled Ns` (single-unit countdown from `rateLimitedUntil`, s/m/h/d, ≤3 chars so it never shifts the quota-bar columns) — reads as auto-recovering, not stuck.
- **"Load X/Y"** → **`Now N (Ww)`**: N in-flight requests, W combined weight, clearly distinct from the `15m`/`1h` throughput counts that follow.
- Focused code-review pass; red→green tests for the countdown + label + the multi-hour-overflow guard.

### 6. Seamless version upgrade — near-zero-downtime reload (single-writer baton) — DONE
**Status:** SHIPPED (v1.5.0). Supervisor-owns-socket + single-writer baton: the supervisor binds :3456 for life and hands the socket to a worker over IPC; on reload it spawns a fresh headless worker and runs a baton so exactly ONE worker holds the writer lease (token refresh / config+state persist / probe) — reads overlap (streams never cut), writes are single-owner (the single-use OAuth refresh token is never double-spent). Any failure falls back to today's tested exit-75 restart (~2s self-heal). Freeze autoUpdate + double-probe during reload.
**Rigor:** brick-safety EMPIRICALLY PROVEN (`reload-refresh-torture` — concurrent refresh across a reload rotates the token EXACTLY once, no invalid_grant). Focused adversarial review = SHIP (single-writer integrity + socket ownership + baton rollback all sound + test-covered).
**Real bugs found & fixed this pass (red→green):** (1) the reload/quit FINAL quota flush was a silent no-op — `persistQuotaState()` ran AFTER `releaseLease()` dropped the lease, so the new primary booted from ≤60s-stale quota and the state generation never advanced (now forced); (2) the forced flush bypasses the cross-worker gen-guard so a racing interval write can't get it refused; (3) test bugs: M4 sent an unhandled SIGUSR2 that terminated the supervisor; the child exit-wait hung the suite; subprocess files contended → now one-process-per-file via `run-tests.sh`. Full suite green.

### 7. Signed-thinking session error-killed behind the FIFO queue (false "all at their limit") — DONE
**Status:** SHIPPED (v1.5.1). User-reported + REPRODUCED 2026-06-27: a thinking session died with "No Claude account can take this request — all N are at their 5h or weekly limit ... beyond the hold window" while `max@dubner.io` (88% free) and `2solarmax` (96% free) sat idle.
**Root cause:** `nextRetryForRequest` (the HOLD-vs-ERROR oracle) iterates accounts via `_matchesRequest`, which excludes ALL Claude accounts for an un-registered newcomer whenever a FIFO queue already exists (fairness gate: `queueState.waiting.length && !queueTicket`). A thinking request also can't use GLM/Kimi, so matchingRoutes=0 → retryAfterMs=Infinity → the server error-fasts (kills the session). The fairness gate is TRANSIENT (it lifts the instant the request registers a queue ticket), but the oracle read it as terminal. The "~1m / beyond the hold window" text was the hardcoded 60s Infinity-fallback, not a real reset.
**Fix:** a thinking newcomer behind the fairness gate now contributes a bounded re-poll hold (`queued_behind_fairness`) so the oracle returns FINITE → it queues and waits its FIFO turn instead of dying. Scoped to thinking-only on purpose: a non-thinking request can fall back to a provider or be cheaply retried, so the gate keeps shedding it as backpressure (an unscoped version regressed the 200-concurrent reload test 0→~54 client timeouts).
**Rigor:** reproduced via a standalone harness BEFORE the fix; pre-mortem (8 failure modes, scoped out admissionPaused + provider leak); adversarial code-review = SHIP; +6 red/green tests (hold-finite, FIFO-advances-to-served, non-thinking-unchanged, admissionPaused-stays-terminal, auth-dead-terminal, weekly-exhausted-not-faked). Full suite green.

### 8. Network interruption (VPN/internet drop) kills a thinking session — DONE
**Status:** SHIPPED (v1.5.2). User-reported 2026-06-27 (same "all N at their 5h or weekly limit" error, but triggered by switching off a VPN / losing connectivity). The 1.5.1 fix was incomplete: it only held when a HEALTHY account sat behind the queue.
**Root cause (two halves):** (a) a network blip cools EVERY account (`markTransientFailure` → finite `cooldownUntil`) and a queue forms; the thinking newcomer is fairness-gated → `_matchesRequest` skips all accounts → their real finite cooldown is LOST → `nextRetryForRequest`=Infinity → kill. 1.5.1 only caught `_isAvailable`-healthy accounts. (b) the in-flight request that ATE the blip was 503'd because `queueAndRetry` refused EVERY `cause==='network'` hold.
**Fix:** (a) for a thinking request, a fairness-gated account now flows into the normal `_retryInfo` bucketing (its real cooldown/rate-limit/5h reset drives a finite hold), never the multi-day weekly reset, never `available:true` (no oracle/selector desync). (b) a STREAMING, replayable network-failed request now HOLDS and resumes when connectivity returns instead of 503; the eventual give-up message is network-honest ("check your internet"), not the quota lie. Non-streaming network failures still fail fast.
**Rigor:** reproduced the all-cooled-down state BEFORE the fix (Infinity→KILL); pre-mortem (2 BLOCKERs caught: weekly-exhausted over-hold + dual available-return guards; MAJOR: server.js network-refusal in-scope); adversarial code-review = SHIP, no must-fix; +5 red/green tests (all-cooled-holds, soonest-cooldown min-merge, oracle/selector agree, streaming-blip holds+resumes, non-streaming fails fast) + rewrote the recovery-network-failure test. 136 tests + full suite green.
**Deferred (named reason):** a NON-thinking session with NO provider configured + all-cooled + queue has the same kill — left unfixed because widening the hold to non-thinking re-breaks the 200-concurrent burst-shed test (#7). Not applicable to this user (GLM/Kimi providers configured). Re-open if a no-provider topology hits it.

### 9. Multi-hour outage strands the whole fleet for 15 min after recovery (no auto-recovery) — DONE
**Status:** SHIPPED (v1.5.3). 2026-06-29: a 5-6h hotel-network nightly cutoff. maxpool correctly HELD the streaming requests through it (the #8 hold), but on recovery every agent stayed dead and a manual restart was needed.
**Root cause:** OAuth access tokens expired during the outage; the required token refreshes failed with `fetch failed`. `markTransientFailure`'s EXPONENTIAL backoff (cap 15 min) + `consecutiveFailures` bump pinned every account at the 15-min cap, so the fleet stayed benched up to 15 min AFTER connectivity returned (and a successful refresh never reset the counter, priming the next failure for the cap). A network outage is fleet-wide — exponential PER-account backoff is the wrong tool.
**Fix:** `markTransientFailure(idx, reason, { network: true })` → short FIXED `networkCooldownMs` (5s), no escalation, no `consecutiveFailures` bump → the fleet retries within seconds of connectivity returning and recovers AUTOMATICALLY (like Claude direct), no restart. Callers: token-refresh `fetch failed` (classified by absence of HTTP status; 429/5xx stay exponential, invalid_grant still disables) + request-level `ECONNRESET`/`terminated`. `networkCooldownMs` in `DEFAULT_SCHEDULER` (not just config — avoids a NaN strand). Added a 10s refresh-fetch timeout so a hung connect can't pin the single-flight refresh. The "recovering" upstream-throttle clears transitively once an account is selectable again (~5s), so no separate fix needed.
**Rigor:** pre-mortem (BLOCKER brick-risk + 2 MAJORs assessed — brick mitigated by single-flight `_refreshPromise` + proven by test; `consecutiveFailures` conflation avoided by not bumping); adversarial code-review = SHIP, no must-fix; +5 red/green tests incl. the gating brick-safety (8 concurrent refreshes → exactly 1 rotation, no `invalid_grant`) + NaN-fallback + classification + exponential-preserved. Full suite green. Caveat (not maxpool's to fix): Claude Code's OWN per-request timeout — if CC gives up client-side during a multi-hour wait, that errored turn needs a re-message, but new requests route instantly.

### 10. Closing the terminal leaves a headless un-quittable orphan; `r` loses the TUI — DONE
**Status:** SHIPPED (v1.5.4). 2026-06-29: closing the maxpool terminal left a supervisor reparented to launchd (PPID 1) still holding :3456, no TUI, un-quittable but by PID; and pressing `r` dropped into raw logs (no TUI). Both rooted in interactive sessions taking the headless seamless-reload path.
**Root cause:** a terminal hangup delivers SIGHUP to the whole foreground group; supervisor + worker both treated SIGHUP as "reload" → the worker reloaded and the supervisor outlived the terminal. And a reload-worker has `useTUI=false`, so any reload (incl. `r`) lost the TUI forever.
**Fix:** SIGHUP is now interactivity-gated (`isInteractiveTerminal()`): in a terminal → drain+exit cleanly (no orphan); headless/service → conventional reload (unchanged — keeps every reload test green). The supervisor forwards NOTHING on an interactive SIGHUP (the worker already got the group signal) — avoids the double-signal force-exit→crash-respawn (a pre-mortem BLOCKER). A terminal-close drain-timeout exits 0 (never misread as a crash to respawn — F2). stdout/stderr EPIPE swallowed + uncaughtException won't exit-75 while draining (write-after-hangup BLOCKER). Interactive `r` now full-cold-restarts (`reloadStrategy`) so the fresh worker re-renders the TUI; `restartWorkerNow` drains in-flight refreshes first (brick-safety). Headless keeps the zero-downtime baton.
**Rigor:** pre-mortem caught both design BLOCKERs (double-signal + write-after-hangup) + a brick gap in the cold-restart path; adversarial code-review = SHIP, no must-fix (216→217 tests, lint clean). New `test/terminal-hangup.test.js` reproduces a real group-SIGHUP hangup via a `MAXPOOL_FORCE_TTY` hook (zero-dep, no pty) and is red without the fix. Known limit: the post-reload TUI *render* can't be CI-tested zero-dep — rides the tested cold-restart path + user verification.

### 11. No persistent logs → incidents un-investigable (e.g. "connection_unavailable across all sessions") — DONE
**Status:** SHIPPED (v1.5.5). 2026-06-30: a fleet-wide `connection_unavailable` hit all sessions a few hours earlier; impossible to diagnose because maxpool persisted nothing (TUI feed is in-memory; request logging opt-in/off).
**Fix:** default-on rotating event log at `<config>.log` (`~/.config/teamclaude.log`) mirroring the `[Maxpool]` console stream (routing, network errors, cooldowns, token refreshes, reloads, upstream-throttle). `src/event-log.js`: fire-and-forget serialized append (never throws into the proxy), bounded queue + drop sentinel, secret redaction + byte-capped lines (<PIPE_BUF for atomic concurrent O_APPEND on macOS), SINGLE-OWNER timer rotation (supervisor or lone unsupervised worker → no reload-window archive clobber), 0600. Teed via console mirror (plain) + `tui._addLog` (TUI mode). Explicit `connection_unavailable (503)` marker for greppability. Opt-out via `eventLog: false`.
**Watcher:** a recurring monitor (cron `230acff9`, every 20 min, durable) reads the log and stays silent unless a real cluster appears (≥2 503s / ≥3-account fleet blip / sustained run), then reports the incident in plain English with the likely cause. Watermark in `~/.config/maxpool-watch.state`. Caveat: cron fires only while a Claude Code REPL is idle (not 24/7) and recurring crons auto-expire after 7 days — re-arm or move to a LaunchAgent for always-on.
**Rigor:** pre-mortem (predicted + drove the multi-writer-rotation + PIPE_BUF fixes) + adversarial code-review (SHIP; should-fix byte-cap closed in-PR with a CJK/emoji test). 7 red/green event-log tests; full suite green (224 tests); existing stdout-grepping tests unaffected by the mirror.

### 12. Overnight `terminated`→503 ("all agents died") = macOS Maintenance Sleep, not the network — DONE
**Status:** SHIPPED (v1.5.6). The recurring `connection_unavailable` finally diagnosed from the #11 event log: the failures were `terminated` (connections cut MID-STREAM, not connect failures), on requests 15-18 min in-flight, all cut in the same second, always 1-3am local. Correlated to-the-minute with the Mac's `pmset` Sleep/DarkWake events — **macOS Maintenance Sleep** powers down the network stack overnight and kills the long streams. NOT the network/NAT (my first guess — wrong, Max pushed back), NOT quota, NOT maxpool. Validated by web search (macOS system sleep drops TCP; `networkoversleep` default 0; display-sleep ≠ system-sleep).
**Fix:** conditional sleep-guard (`src/sleep-guard.js`, macOS-only). While maxpool has work in flight/queued, hold `caffeinate -s -w <pid>` — prevent SYSTEM sleep only (AC power only; DISPLAY still sleeps → screen dark, Max's actual need), tied to the worker pid so it never zombies. Release after a 60s grace once idle → the Mac sleeps normally otherwise (Max explicitly did NOT want blanket always-awake). Wired into acquireLease/releaseLease (only the active lease-holder runs it); poll-based off getGlobalInFlight+queue depth so the wake covers the WHOLE request incl. the multi-minute thinking phase. Config `preventSleep` (default true); disabled in tests via `MAXPOOL_DISABLE_SLEEP_GUARD`.
**Process lesson:** I jumped to a NAT diagnosis and nearly built the wrong fix; Max challenged it, I correlated the event log against `pmset` + web-validated → the real cause. Evidence-first beats plausible-first. The #11 logging is what made this diagnosable at all.
**Rigor:** pre-mortem (verified the two load-bearing concerns — never-drop-mid-request + zombie-safety via `-w pid` — are sound; drove the test-env guard) + adversarial code-review (SHIP after a queueMicrotask CI-lint blocker; folded the child-capture nit). 8 red/green sleep-guard tests (mock spawn, no real caffeinate); full suite green (231 tests); lint clean.

### 13. Load balancer drove ONE account to exhaustion while healthy ones idled; GUI showed stale/`—` quota — DONE
**Status:** SHIPPED (v1.5.7). 2026-07-02: `2solarmax` hit 100%/throttled while 4 accounts sat near-idle; its quota showed `—` in the TUI the whole time, and a `cc all /maxpool` session inherited its "session limit resets 1:50pm". Max was NOT actively working that account through the server.
**Root cause (ONE cause, two symptoms):** (a) the quota prober was **off by default** (`quotaProbeSeconds: 0`), so maxpool only learned an account's 5h/7d utilization from headers of traffic *it* routed — an idle or **out-of-band-used** account (Max was burning 2solarmax in a *direct* non-maxpool session) stayed `unknown`; (b) `_scoreAccount` **preferred** unknown-quota accounts — `_accountScarcity` added pace cost only for non-null windows (null→0) **and** an `explorationBonus = unified7dReset==null ? -0.5 : 0` (lower score = preferred). So maxpool piled load-balanced traffic onto exactly the blind account it couldn't see → drove it to 100%; the GUI showed `—` because nothing refreshed it. The `-0.5` nudge was safe only while the prober quickly resolved `unknown` — with probing off, `unknown` was permanent.
**Fix:** (1) prober **on by default** (`quotaProbeSeconds` 0→60) — polls every account's real 5h/7d from the zero-spend usage endpoint so idle/out-of-band accounts stay fresh (safe: token refresh is single-flight `_refreshPromise` + lease-gated, prober `stop()` drains before baton handoff). (2) `loadConfig` now **backfills top-level keys absent** from an older on-disk config with current defaults (Max's config lacked the key → flipping the default alone would have shipped inert; explicit values, incl. `0`, always win). (3) **removed the `-0.5` exploration bonus** — an unknown account is now neutral, never *more* attractive than a known-healthy one. (4) test opt-out `MAXPOOL_DISABLE_QUOTA_PROBE=1` (mirrors sleep-guard) so spawned integration tests aren't raced by a startup probe.
**Rigor:** pre-mortem via judge (caught the **config-merge-inertness BLOCKER** — the fix would have been a no-op on Max's actual box — + steered off a positive-unknown-scarcity over-correction) + adversarial code-review (SHIP-WITH-NITS; folded a stale prober.js header + a truthy-vs-`==='1'` env footgun in-PR). Red→green: scorer-neutrality + default-on + backfill-absent + preserve-explicit. Full suite green (234 tests).

### 14. Signed-thinking session pinned to an EXHAUSTED account can't fail over cleanly — OPEN (follow-up from #13)
**Status:** OPEN, filed 2026-07-02 (judge flagged during #13 pre-mortem; deliberately out of #13's scope).
**Problem:** a session carrying Anthropic signed-thinking blocks is pinned to its home account (`_migrationSafeForRequest` fails closed for thinking → can't rebalance). When that home account hits its limit, the request falls through the score loop, `_bindSession` re-homes it to a *different* account, and the signed thinking block is replayed to an account that never issued it → Anthropic rejects it (`invalid signature`). #13 prevents *bad new pins* (no longer routes new work toward a blind/over-loaded account) but cannot un-stick an already-pinned thinking session on an exhausted account.
**Proposed fix:** for a `requiresAnthropicThinkingIntegrity` session whose home account is exhausted, return a clean 429 + `retry-after` (hold for the pinned account's own recovery) instead of rebinding+replaying to a stranger. Needs its own pre-mortem + tests.
**UPDATE 2026-07-02 (premise contested — folded into #16):** the "replaying to another account → invalid signature" assumption is **unverified and likely false**. The authoritative Anthropic reference describes the `thinking` block `signature` as a **content/model integrity** check (rejects *modified* blocks; regular thinking blocks "replay across models freely"); it is **never** documented as account/org-bound. maxpool's event log shows `invalid_thinking_signature` has fired **0 times ever**, and the real cross-provider failure (GLM/Kimi ↔ Claude) is explained by model/provider incompatibility, not account identity. If cross-*account* replay works (to confirm empirically), the whole "can't migrate thinking sessions" constraint dissolves and the fix is to *allow* health/time-based rebalancing, not to 429-hold. Superseded by the session-pinning-model redesign (#16).

### 15. `r` (restart) hangs on "Restart pending…" — unbounded pre-restart drain — DONE
**Status:** SHIPPED (v1.5.8). 2026-07-02: pressing `r` stuck on `Restart pending; admission paused while 1 upstream request(s) finish` and never restarted.
**Root cause:** `RestartController.requestRestart()` paused admission then waited for `upstreamRequests.size` to reach 0 with **no timeout** (`restart-controller.js`). A single long streaming/thinking request (minutes) — or a held stream on the 7-day heartbeat — pinned the restart forever. (Earlier "stuck restart" reports were mis-attributed to stale processes; this is the real, reproducible mechanism.)
**Fix:** bounded drain — `requestRestart()` arms a `drainTimeoutMs` (default 10s) force-timer; on overrun it logs and forces `_restart()` anyway (dropped requests reconnect, exactly as the message already promises). Timer is `unref`'d and cleared on natural drain; injectable `setTimeoutFn`/`clearTimeoutFn` for deterministic tests.
**Rigor:** reproduce-first (identified the exact unbounded-wait), red→green tests (force-on-overrun + natural-drain-clears-timer-no-double-restart, fake timer). **End-to-end proof (v1.5.10):** `test/reload-restart-integration.test.js` spawns a real supervisor+worker, holds a request in flight (stub upstream that never responds), fires the REAL `r` restart path headless (gated `SIGUSR2` hook = the TUI key; `config.restartDrainTimeoutMs` bounds the drain fast), and asserts the exact live sequence — `Restart pending (1 upstream)` → `Restart drain timed out; forcing restart` (bounded, the old code hung here) → a fresh worker boot (server came back). This is the scenario Max reported as still-stuck; it now passes. Full suite green (244).

### 16. Session-pinning model: don't strand one account on a long/heavy session — DONE
**Status:** SHIPPED in two parts — v1.5.9 (pace-trigger increment) + v1.5.10 (fail-safe + cross-account thinking migration default ON). Supersedes #14 (now closed). This is the fix for the screenshot Max sent 2026-07-03 (partnerships session drained to 100% while personal sat at 10%): a heavy thinking session pinned to partnerships couldn't migrate and drained its 5h window; with item 1 default-ON, once the pinned account crosses ~90% session (or burns weekly-pace-hot) the thinking session now spreads onto a fresh Claude account. Requires relaunch onto ≥1.5.10.
**Decision (Max):** keep session→account affinity as the default (natural / under-the-radar), but stop it backfiring two ways: (a) a long/heavy session must be able to spread its later load before it exhausts one account; (b) a thinking session must not be permanently stranded on an exhausting account.
**LINCHPIN — empirically confirmed 2026-07-02:** Anthropic `thinking`-block `signature` fields are **content/model integrity, NOT account-bound**. Authoritative docs (rejects *modified* blocks; regular thinking "replays across models freely"; never mentions account) + event log (`invalid_thinking_signature` fired 0×) + a **controlled live replay**: a `partnerships`-signed thinking block, replayed under `personal` (a different Anthropic account), returned **200** (controls: A→A 200, plain-B 200). Max's memory was right — the real signature failure is cross-*provider* (GLM/Kimi ↔ Claude), not cross-account. So thinking conversations CAN move between Claude accounts.
**Critic pre-mortem (judge) scoped it:** fail-safe FIRST → empirical gate (PASSED) → item 1 → item 2 (pace primitive) → defer item 3.
**Increment SHIPPED (v1.5.9):**
- **Item 2 (live):** rebalance trigger `_isBoundAccountHot` moved from raw weekly state to **burn-PACE** (`_weeklyPaceState`, reserve/critical/exhausted) + immediate session-quota pressure — spreads a fast-burner's later load onto fresh capacity before raw exhaustion. Both tier-guard computations in `_shouldRebalanceBoundSession` moved to the **same pace axis** (critic: axis mismatch would make the trigger inert for the fast-burner it targets). Light/near-reset sessions stay pinned (their pace stays normal → never hot). No churn: strictly-healthier-pace-tier + score-margin guards hold (equal-pace-tier → no move, tested).
- **Item 1 (dormant, flag `crossAccountThinkingMigration` default OFF):** `_migrationSafeForRequest` lets a signed-thinking session migrate to another **Claude** account when the flag is on (providers stay barred by `_isRequestCompatible`, so targets are always Claude). Off by default until the fail-safe lands.
**Rigor:** critic pre-mortem (BLOCKING findings folded: fail-safe-first ordering, pace-primitive not raw, axis alignment; item 3 deferred as YAGNI) + empirical linchpin gate (PASSED) + code-review-after (SHIP-WITH-NITS → folded docstring + provider-exclusion test + equal-pace no-flap test). Full suite green (240 tests).
**Fail-safe SHIPPED (v1.5.10):** on `invalid_thinking_signature` for a *migrated* request, `acquireAccount` records `migratedFromName` (the pre-migration issuer); the server.js 4xx branch reverts the session binding there (`revertSessionBinding`), excludes the failed target, and re-forwards PINNED to the issuer (`requestInfo.pinnedAccountName`, honored first in `_selectNext`, falls through if the issuer is down). So a rejected cross-account replay self-heals to the issuer instead of poisoning the session into a 400 loop — defense-in-depth, since replay is empirically portable. `crossAccountThinkingMigration` flipped default ON. Judge pre-mortem (fail-safe design) + code-review-after (SHIP-WITH-NITS, folded comment nits) + red→green tests (migratedFromName-only-on-move, revert+pin routes to issuer, pin-falls-through-when-issuer-down, flag-off-never-migrates). Item 3 (per-session cooldown) deferred — YAGNI until churn observed; existing score-margin + pace-tier guards suffice.

### 17. Rate-limited sessions FAILED instead of HELD; TUI showed rejected accounts as available — DONE
**Status:** SHIPPED (v1.5.11). 2026-07-04: with 3 of 5 Claude accounts exhausted (2 weekly, 1 session) and all sessions using thinking (provider fallback barred), the 2 usable accounts got Anthropic's temporary "Server is temporarily limiting requests (not your usage limit)" 429 under concurrency, and maxpool SURFACED it ("Rate limited") on many sessions instead of holding. Max: "That's the entire point of the load balancer — hold until it can fulfill, not fail."
**Root cause (hold):** `queueAndRetry` (server.js) capped a STREAMING request on the `cause='capacity'` path (which the "temporarily limiting" throttle takes when not promoted to the shared-throttle — correct, since siblings still succeed) at `capacityMaxWaitMs` (15 min), because the `cause==='capacity'` branch was evaluated BEFORE the streaming branch. So a heartbeat-kept-alive thinking session was failed after 15 min instead of held on the SSE heartbeat like an ordinary per-account 429 (which already held 7d) — a backwards asymmetry that failed the MOST-clearly-temporary throttle soonest. The soonest-recovery oracle (`nextRetryForRequest`) already returned the nearest reset (throttle seconds / session-cap ~3h), well under a day.
**Fix (hold):** extracted `computeQueueWindowMs()` (pure, exported for test). A STREAMING `cause='capacity'` request now holds up to `maxWaitMs` (24h) on the heartbeat — so a throttle (seconds) or 5h session-cap (hours) recovers instead of failing; only a multi-day weekly reset (>24h) error-fasts with the honest "add another Claude account" message. Non-streaming still short-capped (no heartbeat); `concurrency_cap` still clamped short (local transient).
**Root cause + fix (TUI):** a `rejected` account (Anthropic actively refusing it — e.g. a per-model weekly sub-limit the general % doesn't expose, like 2solarmax at 54%) rendered as green "active" with a green sub-100% bar → read as available headroom. Now `unifiedStatus==='rejected'` renders the account status as red "blocked" (keys on `a.status`, so it wins even inside an upstream-throttle window); the true utilization number/bar is untouched (not faked to 100% — a rejected account may be SESSION-capped with a genuinely-low weekly).
**Rigor:** judge pre-mortem (BLOCKING: confirmed window-fix not promotion-fix — forcing promotion would open a 7d shared throttle across still-serving accounts; REWORKED Fix B off the fake-100% approach which would misrepresent a session-capped account's weekly) + code-review-after (SHIP-WITH-NITS: equivalence proven exhaustively via truth table; folded stale config comment + rejected-wins-over-throttle). Unit test: `computeQueueWindowMs` truth table (streaming-capacity→24h, non-stream→short, quota→7d, concurrency_cap→clamped). Full suite green (245). NOTE: the durable fix for sustained load is still MORE accounts — holding rides through blips, not a capacity shortfall.

### 18. Per-model (Fable) weekly caps invisible to routing — a Fable request failed instead of balancing to headroom accounts — DONE
**Status:** SHIPPED (v1.5.12). 2026-07-04: Max hit "Fable 5 usage maxed out" on many sessions while 4 of 5 accounts had Fable headroom. Live `/api/oauth/usage` probe confirmed: 2solarmax Fable weekly **100% (critical)** at unified 56%; partnerships 14%, personal 9%, mk@gomokka 31%, max@dubner 0%. Fable was maxed on EXACTLY ONE account — maxpool never balanced it. Max: "I seriously doubt my Fable 5 usage was maxed out across all accounts. How come this wasn't balanced?"
**Root cause:** Anthropic enforces a PER-MODEL weekly sub-limit (usage endpoint `limits[]` → `weekly_scoped` entries with `scope.model.display_name`, e.g. "Fable"), SEPARATE from the unified weekly. maxpool had **zero per-model visibility**: `fetchUsage` parsed only `five_hour`/`seven_day` (+ dead top-level `seven_day_sonnet`), never `limits[]`; routing keyed only on model-agnostic `unified7d`. So a Fable request routed to a 56%-unified account, 429'd, and `markRateLimited` benched the WHOLE account (all models) for the days-long Fable reset — collapsing the pool faster than reality and surfacing the error instead of balancing.
**Fix:** (a) `oauth.js` parses `limits[]` → `scopedWeekly:{fable,opus,...}`; `modelFamily()` maps request models + display names; unified sourced from `limits[]` clean ints (also fixes the `normalizeUsageBucket` 1%→100% misread). (b) `account-manager.js` `_scopedExhausted(account,model)` gate, model threaded into EVERY request-path `_isAvailable`/`_retryInfo` (oracle + selection agree — no spin-loop) → a Fable request skips capped accounts, prefers headroom, the account still serves Opus/Sonnet. (c) `server.js` a model-scoped 429 (contradiction: long reset + unified healthy + known family) benches only `(account,model)`, fails over to headroom; no failure-counter poison. (d) `tui.js` red "Fable maxed" per-account tag (Max's explicit ask).
**Rigor:** critic pre-mortem (SOUND-WITH-CHANGES: thread model through the oracle too; ship only the safe contradiction-classify, no speculative body-parsing without a captured 429) + code-review-after (SHIP-WITH-NITS: folded all 4 — tightened classify vs missing-header FP, model-aware `_bindSession` re-home, no-poison on scoped 429, added missing-header test). `test/scoped-model-limits.test.js` proves the user's exact symptom + oracle-no-spin. Full suite 260 green. NOTE (unchanged): sustained load still needs MORE accounts — per-model routing balances real headroom, it doesn't create capacity.

### 19. Interactive `r` restart HANGS blank — supervisor respawned the worker with a dead socket — DONE
**Status:** SHIPPED (v1.5.13). 2026-07-04: pressing `r` printed "Restarting server now" / "Idle" then went BLANK — no TUI, dead server; Max had to close the terminal EVERY time (5+ occurrences). "You shipped the whole restart flow via R broken."
**Root cause:** the supervisor owns the port via `masterServer` and hands it to the worker over IPC. Once the first worker went primary the supervisor ran `closeMasterAccept()` (`masterServer.close()`); the exiting worker released the last fd. On every exit-75 cold-respawn, `spawnWorker({reload:false})` sent that NO-LONGER-LISTENING server as the handle; the worker ignores a falsy handle (`msg.type===MSG_LISTEN && handle`) → never listened, never became primary, never rendered the TUI → blank hang. `relistenMaster()` ran only ONCE before the supervisor loop; the seamless-reload path re-listened but the cold-respawn path did not. Untested because the integration test is headless → `useTUI=false` → `reloadStrategy` returns 'seamless', so the interactive cold-restart path was never exercised.
**Fix:** (a) `relistenMaster()` at the TOP of the supervisor loop before every cold spawn (idempotent), with bounded EADDRINUSE retry (a just-SIGKILLed worker's fd release is async). (b) Receiver loud-guards closing the whole silent-stranding class: a cold worker handed a falsy handle, or failing to listen/become-primary, now exit-75 → supervisor respawns instead of hanging blank (reload workers still MSG_FAILED → rollback). (c) `MAXPOOL_TEST_FORCE_COLD_RESTART` gated flag to drive the interactive cold path headless.
**Rigor:** REPRODUCED RED→GREEN with a real supervisor+worker end-to-end test (`test/reload-cold-restart-respawn.test.js`) — two consecutive cold restarts each must re-bind + serve a 200; verified failing without the fix, passing with it (5/5 + 2-cycle stable). Judge review (SHIP-WITH-NITS) caught two real off-interactive-path issues — the seamless-fallback EADDRINUSE supervisor-crash and the silent cold-start stranding — both folded via (a)+(b). Full suite 261 green. NOTE (Q5 gap): the headless test proves re-bind + `becomePrimary` (root cause is upstream of `tui.start()`); the literal TUI re-render is not automated (no pty in-suite).

### 20. A per-model (Fable) rejection blocked the WHOLE account — display + routing conflation — DONE
**Status:** SHIPPED (v1.5.14). 2026-07-05: after restart, 2solarmax showed status **"blocked"** + "Wk exhausted 72%" + "Fable maxed" and was benched for ALL models, though only its Fable weekly was capped (unified weekly just 72%). Max: "why is blocked — smth only relevant to one specific model — showing in the same column as general status making it seem like the whole account is unusable."
**Root cause:** Anthropic returns `anthropic-ratelimit-unified-status: rejected` on a per-model (Fable) 429. `updateQuota` (called on EVERY response incl. the 429, server.js:449) wrote that account-wide `unifiedStatus='rejected'` unconditionally; `_weeklyRawState` had `if (unifiedStatus==='rejected') return 'exhausted'` → `_isAvailable` benched the whole account (Opus/Sonnet too) + TUI rendered "blocked" + "Wk exhausted 72%". `unifiedStatus` is persisted, so it survived restart, and a benched account never gets a 200 to clear it → stuck until the weekly reset (~1.5 days). A per-model cap conflated into an account-wide block, in both routing AND display. (The v1.5.11 "blocked"-on-rejected display and the v1.5.12 per-model routing were fighting: v1.5.12 correctly benched only Fable via scopedWeekly, but the same 429's headers still poisoned the account-wide status.)
**Fix:** `_isAccountWideRejected(account)` — `unifiedStatus==='rejected'` counts as account-wide ONLY if a unified bucket corroborates (`unified5h`/`unified7d` ≥ 0.985); a bare 'rejected' with healthy unified is a per-model sub-limit (tracked in scopedWeekly). Routed `_weeklyRawState` + the TUI "blocked" label through it; `updateQuota` also stops recording 'rejected' account-wide when unified is healthy. So 2solarmax shows "active" + "Fable maxed", keeps serving Opus/Sonnet, and Fable routes to headroom accounts. The genuine account-wide bench safety-net is unchanged (the independent 429 throttle path benches account-wide when a rejection is NOT model-scoped).
**Rigor:** RED→GREEN proven (reverting the read-site guard fails the restored-stale-rejected test = the exact post-restart symptom). Judge review = SHIP (verified: genuine account block still benched via the throttle path; no lost signal; session-cap gate independent). Full suite 264 green.

### 21. Resuming a `cc glm` session under `cc all` 400'd — no cross-provider session pinning — DONE
**Status:** SHIPPED (v1.5.15). 2026-07-08: `cc glm` sessions talk directly to z.ai and accumulate GLM tool-use ids (`call_…`); resuming one via `cc all` (through maxpool, profile=all) → (1) Claude Code can't restore model `glm-5.2` → falls back to opus (cosmetic, client-side), and (2) maxpool routed the GLM-history request to an Anthropic account → **400: server_tool_use.id must match ^srvtoolu_**. Max: "make cc all work for all session types; glm→glm/kimi only; claude→claude with cross-fallback only when all claude down; add a TUI setting to control it."
**Root cause:** maxpool had zero home-provider awareness — it routed purely on the profile gate + priority. Worse, a GLM transcript carries (unsigned) thinking blocks → the existing `requiresAnthropicThinkingIntegrity` fired → **barred providers → force-pinned the GLM session to Claude → the 400** (the thinking heuristic actively MISROUTED GLM sessions). Empirical fingerprints (verified on real transcripts): Anthropic=`toolu_`/`srvtoolu_`, GLM(z.ai)=`call_`, Kimi(moonshot)=`tool_`; thinking signatures are NOT a reliable discriminator (5% of GLM blocks are signed, Kimi 100% signed). The id-prefix allowlist `^(toolu|srvtoolu)_` is the load-bearing signal. PIN, don't translate: a GLM/Kimi transcript can never be made Anthropic-valid (can't manufacture `srvtoolu_` or a valid signature); Claude→GLM/Kimi is safe.
**Fix:** `detectTranscriptOrigin` (server.js) classifies each request from its replayed tool-use ids → `requestInfo.originClass` ('foreign'|'anthropic'|null) + `originProvider`. `_isRequestCompatible` (account-manager.js, the single routing chokepoint) gates: a **foreign** session HARD-bars oauth (provider-only, regardless of policy — and this branch SUPERSEDES the thinking bar, fixing the misroute); an **anthropic/unknown** session prefers Claude with policy-gated provider fallback. `scheduler.crossProviderFallbackPolicy` = `never` | `when-exhausted` (default) | `always`, cyclable live in the TUI Routing menu (`m`→`f`, no confirm) — `always` peers providers via `_effectivePriority`. Foreign origin is sticky per session (latches, never downgrades) so a no-tool follow-up turn stays pinned; the selector AND the retry oracle both read it (no desync). Honest `unavailableMessage` for a pinned-but-exhausted foreign session. (Error #1, the model-restore warning, is client-side and cosmetic — maxpool still routes the session to GLM correctly by its transcript.)
**Rigor:** exhaustive 5-reader investigation workflow (routing, empirical fingerprints, provider-fallback flow, TUI/config, online research on id/signature validation + prior art) → judge pre-mortem (SOUND-WITH-CHANGES: R1 foreign-before-thinking-bar to avoid Infinity-strand, R2 sticky-read so selector/oracle agree, R3 honest error, R4 make `always` real not a no-op, R5 widen saveConfig to persist scheduler — all folded) → 16-test suite incl. the 400-prevention invariant, the misroute-fix, sticky latch, all 3 policies, and the describeRequest seam → judge code-review-after (SHIP-WITH-NITS: added the seam test, dropped dead `originScanned`). Full suite 280 green.

### 22. v1.5.15 OVER-PINNED: Kimi/GLM sessions wrongly barred from Claude — narrowed to react-and-heal — DONE
**Status:** SHIPPED (v1.5.16). 2026-07-08: Max: "That's idiotic. Kimi and Claude are compatible! Kimi sessions can go to Claude and vice versa and can also go to GLM. Send to Kimi if no Claude available." v1.5.15's "any foreign tool-use id → provider-only pin" was too strict.
**Root cause of the over-strictness:** Anthropic validates a client `tool_use.id` LOOSELY (`^[a-zA-Z0-9_-]+$` — so GLM `call_…` / Kimi `tool_…` PASS); only `server_tool_use.id` is strict (`^srvtoolu_` — the actual 400). Empirics (200 GLM + 200 Kimi + 120 Claude transcripts): server_tool_use is rare (GLM 29/200, **Kimi 0/200**, Claude 0/200). So Claude/GLM/Kimi interoperate for ordinary sessions; only a foreign `server_tool_use` (or a rejected thinking signature) truly can't replay to Claude. v1.5.15 also barred a Claude signed-thinking session from GLM/Kimi fallback — wrong per "send to Kimi if no Claude available".
**Fix (predict-the-deterministic-case + react-and-heal):** `detectTranscriptOrigin` now flags `anthropicIncompatible` ONLY for a `server_tool_use` id ≠ `srvtoolu_` (+ a `homeProvider` soft hint). Dropped the client-tool_use / tool_result / unsigned-thinking predictive bars. For everything uncertain (a rejected thinking signature), **react-and-heal**: the 400 is pre-stream + buffered, so on an Anthropic-account 400 matching the incompatibility shapes, maxpool latches the session provider-only (sticky) and transparently retries on GLM/Kimi — the client never sees it. Removed the unconditional thinking→bar-provider (a lenient provider accepts an Anthropic signature); a signed-thinking session now falls to GLM/Kimi under `when-exhausted`, but its live cross-account MIGRATION stays Claude-only (rebalance guard). Net: a Kimi/GLM-without-server-tools session runs on Claude; a Claude session falls to GLM/Kimi when Claude is down; only the genuine `server_tool_use` 400 pins to providers.
**Rigor:** judge pre-mortem (SOUND-WITH-CHANGES: react-and-heal is the primary mechanism, predict only the deterministic case; 4 must-fixes — drop unsigned-thinking bar, new self-heal code path, broaden the error classifier, keep signed-thinking migration Claude-only — all folded) → rewrote 16 cross-provider tests + updated 2 thinking tests + added an HTTP react-and-heal integration test (Claude 400s server_tool_use → self-heals to GLM, client gets 200) → judge code-review-after (SHIP-WITH-NITS: tightened the classifier against false-positives, added no-provider + classifier-guard tests). Full suite 281 green.

### 23. Providers showed no Ses/Wk limits; "Fable maxed" mislabelled 90%; scoped tag freshness unverifiable — DONE
**Status:** SHIPPED (v1.5.17). 2026-07-08, three linked asks: (A) "Why don't GLM and Kimi show session/weekly limits like the others?" (B) "double-check Ses/Wk are actually correct — I saw a brief 95% weekly with no account at that limit now." (C) "Fable shows maxed on one account — I'm not sure it's true; how do you ensure it's true and refreshed?"
**Root cause (B/C):** the "Fable maxed" tag AND the routing bench (`_scopedExhausted`) both fired on `severity==='critical'`, which Anthropic labels a scoped weekly at ~90% — well before it's capped. Live state confirmed max@dubner Fable=90%/critical/active: real data, but "maxed" overstated it and the account was hard-benched for Fable with ~10% headroom left. (A): provider accounts had no quota probe at all — z.ai has a pollable monitor endpoint; Kimi's coding key has none.
**Fix:** (1) `_scopedExhausted` + the TUI tag now share ONE predicate — `isActive && utilization >= weeklyExhaustedThreshold (0.985)` — so "maxed" (red) renders iff actually benched, and 0.85..<0.985 renders "Fable NN%" (yellow). A real 429 writes util:1, still caught. (2) STICKY reactive-429 (load-bearing): `markRateLimited` marks the scoped entry `reactive`; `applyUsageData` won't let a lagging 60s probe lower/drop it while its resetAt is future — without this, dropping the severity clause caused a 1-per-60s un-bench→re-429 flap. (3) z.ai quota probe → SEPARATE `providerSes/providerWk` fields (never the OAuth `unified*` gates); Kimi → honest "console-only" label; TUI renders provider Ses/Wk bars. (4) staleness marker: `lastProbeOkAt` stamped on every successful probe; TUI shows "stale" when the background probe hasn't succeeded in > 2× interval (answers "is it refreshed?"). (5) soft scoped-weekly de-preference in `_scoreAccount` sheds Fable load off a hot account before the 0.985 cliff (was absent from all scoring).
**Rigor:** judge pre-mortem (SOUND-WITH-CHANGES — the sticky-reactive co-change was the load-bearing must-fix it caught) → 27 RED→GREEN tests (predicate, sticky-429 four cases, provider isolation, z.ai envelope parse, staleness, soft scoring, credential-hop) → judge code-review-after (SHIP — all six attack vectors verified; closed the one coverage-gap nit in-PR). Full suite 284 green.

### 24. Load balancer kept a bound session pinned to a 90%-weekly account while healthier accounts idled — DONE
**Status:** SHIPPED (v1.5.18). 2026-07-09. Max: "why does the LB keep hitting max@dubner (~90% weekly, resets in ~2 days) while less-utilized accounts exist?" Live state confirmed: max@dubner unified-weekly 90% (RAW reserve / PACE critical, resets 39h) still drawing traffic while mk@gomokka (17%, normal) was far healthier.
**Root cause:** a *bound* session (sticky) only leaves its account if `_shouldRebalanceBoundSession` fires, whose gate `bestScore <= boundScore * REBALANCE_SCORE_MARGIN(0.5)` requires the alternative to score ≤ HALF the bound account's score. For an IDLE bound account boundScore ≈ the ~2 concurrency floor, so the threshold ≈ 1 sits BELOW every account's minimum score — mathematically unsatisfiable → the session never rebalances. The margin was calibrated for concurrency relief (moving off a LOADED account), not weekly-exhaustion relief (moving off an idle-but-near-cap account). The reserve BAND (85–95% RAW) is the trap: at ≥95% the bound account goes ineligible and auto-rehomes; below 85% it's healthy; 85–95% stays eligible AND can't rebalance.
**Fix (bound-session-scoped, judge pre-mortem SOUND — no must-fix):** (1) `_shouldRebalanceBoundSession`: when the bound account is genuinely near-cap (RAW reserve+), skip the concurrency margin and move — every rebalance candidate is already RAW normal/soft (a strict absolute-headroom improvement, flap-stable: a reserve account can never be a target → no bounce-back). Keep the steep margin + pace-tier guard for the pace-only-but-RAW-healthy fast-burner (a 79%-soft/pace-critical account isn't churned; a RAW-soft/pace-normal fast-burner never even reaches the gate). (2) Unified the acquire-path `weeklyPasses` to the healthy-first ladder for bound sessions too, so a re-home lands on a healthy account instead of re-picking the idle reserve account. Signed-thinking safety preserved: the rebalance loop skips PROVIDER targets, so `return true` only fires with a Claude target present (no healthy Claude → stays put, never shuttled to a provider).
**Rigor:** RED reproduction (5 tests: idle-90%-rebalances, live-5-account scenario, 69%-fast-burner-NOT-churned, no-stranding, 96%-critical-moves) → judge pre-mortem (SOUND, all 7 attack vectors held; validated NOT adding a `bestScore<boundScore` guard which would re-break it) → judge code-review-after. Full suite 289 green + isolated reload suites green; eslint 0 errors.

### 25. A healed network blip showed "Err fetch failed" on a provider for ~100 min (phantom error) — DONE
**Status:** SHIPPED (v1.5.20). 2026-07-10. Max saw `glm-fallback`/`kimi-fallback` showing `Err fetch failed` and asked why. Investigation: both providers' upstreams are reachable (z.ai 401, kimi.com/coding 400), tokens valid (z.ai quota probe returns 200), DNS resolves. The event log showed provider `fetch failed` exactly twice (16:37, 19:42 on 07-09), each time BOTH providers within ~5-27ms — a local connectivity blip, not a provider fault; OAuth accounts saw the same. `connection_unavailable (503)` count = 0, so ZERO client impact. The screenshot was 1h40m AFTER the last blip.
**Root cause:** `markTransientFailure(…, {network:true})` sets `lastError` + a 5s cooldown but deliberately does NOT bump `consecutiveFailures` (a fleet-wide blip isn't the account's fault). The 5s cooldown expires and the account goes healthy — but nothing clears `lastError`. The `rate_limited`/`upstream_throttled` recovery paths in `_isAvailable` DO clear it; the network path didn't. So the error is orphaned: it lingers in the TUI until the account's next request overwrites it — and an idle fallback provider (Claude healthy → no fallback traffic) never gets one, so it showed the ancient blip until restart.
**Fix:** `_clearRecoveredNetworkError(account, now)` called for every account in `refreshExpiredQuotas` (runs on the TUI loop + request path, so it reaches idle providers — `_isAvailable` never evaluates an out-of-candidate-set provider). Clears `lastError`/`lastErrorAt` (+ nulls the now-past `cooldownUntil`/`provisionalUpstreamUntil`/fingerprint) once the account is active with `consecutiveFailures===0` and no live backoff window. The `cf===0` guard keeps a genuinely-flaky account (windowless `markResult` failure, counter bumped) showing its error. Display-honesty only — no routing/logic reads `lastError` except the `rate_limited`/`upstream_throttled` string-equality checks, which are excluded by the `status active` gate.
**Rigor:** RED reproduction (6 tests: healed-blip-clears, retained-during-cooldown, idle-provider-clears, flaky-account-keeps, auth-dead-keeps, throttled-keeps) → judge pre-mortem (SOUND-WITH-CHANGES — 3 must-fixes: wire `now`, null sibling windows, correct the `upstream_throttled` rationale — all folded) → judge code-review-after. Full suite green, eslint 0 errors. NOT a bug in routing/quota — a display-staleness fix (same class as the 1.5.17 "stale" quota marker).

### 26. Fallback providers (glm/kimi) vanished from the TUI after a restart — DONE
**Status:** SHIPPED (v1.5.21). 2026-07-10. Max: "After the restart Kimi and GLM do not show at all." Footer read "5 account(s)" — only the config OAuth accounts.
**Root cause:** the fallback providers are created LAZILY inside the request handler by `prepareRuntimeProviders(am, req.headers)` from the `x-maxpool-zai-token`/`x-maxpool-kimi-token` headers the external `cc all` shell wrapper sends (it reads the tokens from Keychain). They're runtime accounts, NOT persisted like the config OAuth accounts — so a restart with zero requests loads only the 5 config accounts; the providers don't reappear until the next `cc all` request re-sends the headers.
**Fix (persist + restore in the existing state.json):** `exportRuntimeProviders()` serializes the runtime provider accounts (incl. token, upstream, modelMap) and `restoreRuntimeProviders()` re-creates them via the same `upsertRuntimeAccount` the header path uses (so a restored provider is byte-identical to a header-created one). Wired into the existing `persistQuotaState` (state.json payload gains `runtimeProviders`; the sole saveState caller, covers the 60s interval + all forced flushes) and the boot restore (after `restoreQuotaState`). A live `cc all` request still refreshes the token before routing (prepareRuntimeProviders runs ahead of selection), so a stale restored token never serves. state.json is 0600, same as the OAuth-token-bearing config — no new exposure. Provider quota (providerSes/Wk) isn't persisted (excluded from PERSISTED_QUOTA_FIELDS in 1.5.17) so it's re-probed. Cold restart fully fixed; a seamless-reload worker created <60s before reload self-heals on the next `cc all` request (documented).
**Rigor:** RED reproduction (7 tests: export filters, restart round-trip, malformed-skip, header-override, empty-safe, REAL saveState→loadState→restore via temp MAXPOOL_CONFIG, backward-compat for pre-fix state files) → judge pre-mortem (SOUND, no must-fixes; all 7 attack vectors — restore-order/quota, restored≡header-created, generation-baton, reload-handoff, idempotency, staleness, persist-vs-keychain — verified) → judge code-review-after. Full suite green, eslint 0 errors.

### 27. Provider (GLM/Kimi) rows misaligned + no weekly/quota shown; reload-integration flaky — DONE
**Status:** SHIPPED (v1.5.22). 2026-07-10. Three asks in one.
**(a) De-flake `reload-integration.test.js` (TWO saturation-induced modes):** (1) TIMEOUT — the in-flight stream already had 60s but warm-up `waitFor` + cutover `waitForLine` + helper defaults were 20s → under 8+ parallel real-spawn saturation the boot/cutover exceeded 20s. Fix: helper defaults 20s→45s, warm-up/cutover 20s→60s, and line ~201's post-swap `proxyStream` (its OWN 20s default, not the helper) → explicit 60s; sibling fail-fast caps left at 20s. (2) TRANSIENT ECONNREFUSED — surfaced only when the release ran `run-tests.sh` (reload test right after the saturating fast batch): 1 of 20 cutover connects got refused. Judge read the handoff: it's `masterServer.close()` + `relistenMaster()` re-arm (NOT a permanently-bound backlog), so the release→re-arm→listen ordering has a sub-ms overlap window a saturated connect can catch — self-clearing, client-retry-covered, zero user impact (a rare isolated prod reload's window is astronomically smaller). Fix: the hammer retries a refused connect ONCE after 75ms and counts only a SUSTAINED refusal (retry also fails) — the real "no dropped connection" guarantee, not an overly-strict zero-tolerance the close+rebind handoff doesn't provide. A genuinely-dropped socket still fails both → still caught. Durable core option (bind-before-release / SO_REUSEPORT overlap) noted as follow-up. Verified `run-tests.sh` 3/3 clean.
**(b) Why GLM shows no weekly / Kimi no quota (researched):** z.ai's GLM Coding Plan DOES have a weekly quota (5h + 7-day), and `/api/monitor/usage/quota/limit` returns it — but z.ai intermittently OMITS the weekly entry (live probe returns only the 5h token window + the monthly MCP tool cap). `classifyZaiLimit` already handles the weekly when present, so GLM's Wk populates whenever z.ai sends it. Kimi: Moonshot's balance API (`/v1/users/me/balance`) 401s for the coding-plan key (it's a pay-as-you-go platform endpoint) and `api.kimi.com/coding/*` has no usage path — so "console-only" is genuinely correct, not fixable. No sourcing change needed.
**(c) Visual alignment (the real bug):** two-fold — `_renderProviderAcct` rendered Act/OK/Fail/Last BEFORE the bars (bars in a different column than OAuth), AND `type = a.type.padEnd(7)` while "provider" is 8 chars (every provider row shifted 1 char). Fixed BOTH: restructured the provider row to place Ses/Wk right after status (same columns as OAuth), added `emptyBar(label,w)` (mirrors bar()'s null branch exactly) for absent quota — GLM Wk="—", Kimi="n/a"+console-only, probing="probing" — `showBoth`-gated, `loadText`+compact `Last` trailing, `Lim` restored; and `type.padEnd(7)`→`padEnd(8)`.
**Rigor:** judge pre-mortem (both SOUND-WITH-CHANGES — 3 must-fixes: line-201 explicit timeout, showBoth gate, emptyBar exact width — all folded; + a Consider to keep `Last`) → 4 new render tests incl. a column-offset alignment assertion → judge code-review-after. Full suite green, eslint 0 errors.

### 28. In-flight lease LEAK (106 phantom "active", 2 accounts stuck at the cap) + Kimi quota — DONE
**Status:** SHIPPED (v1.5.23). 2026-07-11. Max: "shows 106 active sessions — impossible" + "find where/how to get Kimi coding-plan quota, I'm using the coding key."
**(a) CRITICAL lease leak:** live state confirmed `globalInFlight:107`, max@dubner + 2solarmax both pinned at inFlight=50 = `safetyMaxActivePerAccount` → `_isAvailable` false at the cap → those accounts falsely "full", routing broken; Activity showed requests from 5+ HOURS ago still in-flight. Root: `streamResponse` loops on `await reader.read()` with NO idle timeout — a half-open upstream (headers, then silence, never closes) blocks the read forever; the whole handler `await`s that, so neither `releaseAccount` NOR the `finally` `onRequestEnd` runs → both the account inFlight AND the "active" feed leak. `res.off('close')` at :442 removes the disconnect-abort once headers arrive, and the loop only checks `res.destroyed` AFTER a read resolves, so a blocked read was never interrupted. Fix (server.js): (1) `res.once('close', () => reader.cancel())` unblocks a blocked read on client disconnect; (2) per-chunk idle race (reset every chunk → a slow-but-alive stream is never cut) → UPSTREAM_IDLE on stall; (3) UPSTREAM_IDLE/TTFB/BODY added to `isTransient` → committed streams fall through to `sendErrorResponse` (writes `event: error` + `res.end()` → frees the lease AND unhangs the client); (4) backstops for the other two unbounded awaits — fetch TTFB (dedicated AbortController via `AbortSignal.any`, cleared once headers arrive so streaming is never cut) + non-streaming `arrayBuffer` timeout. Env-overridable (MAXPOOL_TTFB_MS/STREAM_IDLE_MS/BODY_MS; 120/300/300s, floors). Current stuck state clears on reload (fresh worker, inFlight=0, transient).
**(b) Kimi coding-plan quota (I was WRONG that it's console-only):** the coding key (sk-kimi-…) DOES expose usage at `<base>/v1/usages` (Bearer + `User-Agent: KimiCLI/1.6`) — my earlier probe hit `/users/me/balance` (Open Platform, 401s a coding key). Live-confirmed: `usage{}`=weekly + `limits[]` 300-min window=5h. Added `fetchKimiUsage` → Ses/Wk bars like z.ai (live: Ses 0% / Wk 10%). CRITICAL parse guard: utilization is `null`, never a computed NaN — else `clamp01(NaN)=0` renders a blind account "0% = fully available" and routes real traffic onto quota it can't see. Partial response (usage absent) → {error} → keep last-known (not a blanked bar).
**Rigor:** judge pre-mortem (both SOUND-WITH-CHANGES — must-fixes: client-hang-on-idle-end via sendErrorResponse, per-chunk clearTimeout, config-driven idle default, backstop fetch+arrayBuffer, NaN→null trap, timeUnit normalize — ALL folded; empirically verified fetch surfaces the custom UPSTREAM_TTFB code) → RED→GREEN tests (4 stream-leak incl. idle-resets-per-chunk + 9 Kimi) → judge code-review-after. run-tests.sh green, eslint 0 errors.

### 29. Dead refresh token (invalid_grant) → 60s retry STORM + generic "error" (no re-auth signal) — DONE
**Status:** SHIPPED (v1.5.24). 2026-07-11. Max: "restarted, two accounts show error." Investigation: max@dubner + 2solarmax refresh tokens are DEAD — every refresh returns `400 invalid_grant: "Refresh token not found or invalid"`. These are permanently dead (Anthropic-side / a prior double-spend, indistinguishable here) — ONLY browser re-auth revives them; no code can. But maxpool made it worse: `markAuthFailed` set `status='error'` yet NOTHING stopped the retry — the prober (`probeAll` filtered only `type==='oauth' && credential`, not status) probed the error account every 60s → `ensureTokenFresh` → token still expired → refresh again → 400 again, FOREVER (15:21→15:25+), re-POSTing the dead token (+ a double-fire 200ms after each failure). Pointless, noisy, and hammering Anthropic's OAuth endpoint with a dead token risks looking like abuse.
**Fix:** `account.refreshDead` latch, set ONLY in `ensureTokenFresh`'s non-retryable (rejected-refresh) branch — never in `markAuthFailed` (shared with provider auth failures; a request-401 path with a still-valid refresh token must stay recoverable). Guard `if (account.refreshDead) return false` stops the retry; the prober skips `refreshDead` accounts (the storm's sole source); a transient/network refresh failure does NOT latch (stays recoverable). Recovery: cleared on browser re-auth — CRITICAL judge catch: the real re-login path is the in-place `_upsertOAuthAccount` (tui.js), NOT `updateAccountTokens` (the index.js reconcile sees no change and never calls it), so `refreshDead` is cleared there too — else a re-authed account would stay bricked despite valid tokens. TUI shows `reauth` (re-login needed) instead of a generic `error` — DISPLAY-ONLY (`account.status` stays `error` so routing/eligibility still exclude it). **User action for the current 2 dead accounts: re-login via the TUI `l` key** — no code revives a dead refresh token.
**Follow-up (tracked, not this fix):** possible self-inflicted double-spend across this session's rapid restarts (rotate-in-memory → persist window) — persist the rotated refresh token synchronously before serving on it, to shrink the rotate-not-persisted-across-restart window.
**Rigor:** judge pre-mortem (SOUND-WITH-CHANGES — 3 must-fixes: the in-place re-login clear [would've shipped a permanently-unrecoverable account], reauth display-only, upsertRuntimeAccount clear — ALL folded; confirmed the set-site is correct because there's NO oauth request-401→markAuthFailed path, it's provider-only) → RED→GREEN (6 dead-token + 1 TUI) → judge code-review-after. run-tests.sh green, eslint 0 errors.

### 30. Browser-login flow read as "add new account" even when it re-authenticated in place — DONE
**Status:** SHIPPED (v1.5.25). 2026-07-11. After the #29 fix, Max needed to re-login the 2 dead accounts but "I do not see a re-login option — the whole flow is designed to log in to a NEW account." Truth: the flow ALREADY re-auths in place (dedupes by `accountUuid`, then name) — logging into an existing account revives it with no duplicate — but nothing SAID so: the confirm said "add any Claude account", it always prompted "Name this account", and always printed "Added account X" (even on an in-place update). Copy/flow-only fix (dedup/routing untouched): `_findExistingOAuthAccount(profile)` detects an existing account before the prompt (same uuid→email precedence as the upsert); when found, skip the name prompt, keep its name, say "This Claude account is already added as <name> — re-authenticating it in place (no duplicate)"; else "Name this NEW account". The terminal message is driven by the upsert's returned `updated` in BOTH branches (judge must-fix — a manually-typed name colliding with a different account updates in place → reads "Re-authenticated", never a false "Added"). Confirm title "Log in / re-authenticate via browser?" + detail; footer "Login/re-auth (browser)".
**Rigor:** judge pre-mortem (SOUND-WITH-CHANGES — must-fix: drive the final message off `updated` in both branches; guard the email fallback on a truthy email) → RED→GREEN (4: match precedence, re-auth-in-place revives + no duplicate, add-new, name-collision) → judge code-review-after. run-tests.sh green, eslint 0 errors.

## DONE (this session, for context)

- Keychain import fix (1.0.1) — **to be reverted per #3.**
- Quit/drain bounded to 15s so `q`/Ctrl-C actually quits under load (1.0.2).
- Backward-compat `x-teamclaude-*` headers so running sessions survive the rename (1.0.3).
- TUI active-account indicator + browser login (`l`) + rename (`n`); CLI `rename` (1.0.5).
- README setup guide + FAQ + account-risk/ToS disclaimer (1.0.6 / 1.0.7).
- Queue: weekly caps now wait+retry instead of fail-fast; non-stream cap, capacity clamp, backpressure, stale-head reaper, poll jitter, honest messages (1.1.0).
- ToS review (4-advocate council + judge): personal use = contested gray area; public promotion = do-not. Launch held.
